# HRMS-404 测试账号清单与逐项验收步骤

> 配套文档：[README.md](README.md) · [开发文档.md](开发文档.md)
> 适用版本：v4.0.4 ｜ 访问地址：<http://localhost:8080/login>
> **所有演示账号的密码均为 `123456`**（与建库触发器、登录页提示一致）

---

## 一、账号速查表

### 1. 四个主测试账号（建议按此顺序测）

| 登录名 | 密码 | 角色 | 绑定员工 | 所属部门 / 职位 | 能看到的菜单 |
|---|---|---|---|---|---|
| `admin` | `123456` | 系统管理员 | 未绑定员工 | — | 全部（含**账号管理**） |
| `hr01` | `123456` | 人事专员 | E0740 周慧 | 人事部 / 人事专员 | 全部业务模块 + **账号管理（受限）** |
| `manager01` | `123456` | 部门经理 | E0394 郭杰 | 销售一部 / 销售经理 | 员工管理 / 考勤 / AI 查询 / 我的中心（**仅本部门数据**） |
| `E0001` | `123456` | 普通员工 | 何琳 | 研发一组 / 研发经理 | 首页 / 考勤打卡 / 我的中心（**仅本人数据**） |

### 2. 批量账号（用于测数据量、分页、导出）

| 范围 | 说明 |
|---|---|
| `E0001` ~ `E1002` | 每个在职员工都有账号，**登录名 = 工号**，密码 `123456` |
| 其中 979 个启用 | 对应在职员工 |
| 其中 21 个已禁用 | 对应已离职员工，例如 `E0069`（高婷）、`E0082`（徐娜）、`E0159`（高轩） |

> 账号总数 **1003** = 1002 名员工 + 1 个不绑定员工的 `admin`。

### 3. 反向测试专用账号

| 登录名 | 密码 | 预期结果 |
|---|---|---|
| `E0069` | `123456` | ❌ 登录失败：**账号已被禁用（可能已离职），请联系 HR** |
| `admin` | `wrong123` | ❌ 登录失败：**密码错误，请重试** |
| `nobody999` | `123456` | ❌ 登录失败：**账号不存在** |

---

## 二、逐账号测试步骤

### 测试 1：`admin`（系统管理员）

- [ ] 登录后左上角显示角色徽章 **系统管理员**，头像显示「管」（未绑定员工时取默认字）
- [ ] 侧边栏出现 **系统管理 → 账号管理**，且**有**「账号管理」入口
- [ ] 首页统计卡片正常显示数字（员工总数、部门数等）
- [ ] 账号管理页顶部显示 **共 1003 个账号**
- [ ] 账号列表按角色优先级排序：管理员 → 人事专员 → 部门经理 → 普通员工
- [ ] 「每页 5 / 10 / 20 条」切换正常，上一页/下一页边界置灰正确
- [ ] 关键词搜 `E0001`、搜「周慧」、搜 `hr` 均能命中
- [ ] 角色筛选「系统管理员」→ 只剩 `admin` 一行
- [ ] 状态筛选「禁用」→ 出现 21 条离职员工账号
- [ ] AI 智能查询：问「各部门有多少在职员工？」，约 1~2 秒内返回 SQL + 结果表格
- [ ] AI 智能查询进阶：问「查询研发中心及其所有子部门的员工总数」→ 应生成 **`WITH RECURSIVE`** 递归 SQL，结果为 **281**（本库部门树为 3 层：总公司 → 研发中心 → 研发一组/二组/三组）
- [ ] 顶部右侧「修改密码」可正常弹窗改密（改完记得改回 `123456`）

### 测试 2：`hr01`（人事专员）

- [ ] 侧边栏**有**「账号管理」入口（HR 可进入）
- [ ] 员工管理：新增一名员工 → 保存后列表出现该员工
- [ ] **新增员工的账号是触发器自动创建的**：去账号管理搜该工号，能看到账号，角色为「普通员工」，密码 `123456`
- [ ] 部门管理：可增删改（注意编制预算触发器会对超预算的职位操作报错）
- [ ] 职位管理：新增职位时若超出部门编制预算，应被触发器拦截并提示
- [ ] 薪资管理：点「生成当月薪资」对本已存在的月份应提示**当月薪资已全部生成**
- [ ] 账号管理中点 `admin` 那一行 → **操作列应显示「—」，没有改角色/重置密码/禁用按钮**
- [ ] 在「开通账号」弹窗里，角色下拉**只有**「普通员工 / 部门经理」两个选项（HR 不能授予管理员/人事）

### 测试 3：`manager01`（部门经理）

- [ ] 侧边栏**没有**「部门管理」「职位管理」「薪资管理」「账号管理」
- [ ] 员工管理只显示**销售一部及其子部门**的员工，看不到研发中心等其它部门
- [ ] 考勤汇总只统计本部门范围
- [ ] 直接手输网址 <http://localhost:8080/users> → **403 页面**（越权拦截）
- [ ] 直接手输网址 <http://localhost:8080/salaries> → **403 页面**

### 测试 4：`E0001`（普通员工）

- [ ] 侧边栏只有：首页 / 考勤打卡 / 我的中心
- [ ] 我的中心能正确显示：工号 E0001、何琳、研发一组、研发经理、岗位工资、入职日期
- [ ] 上班打卡 → 提示成功，状态显示「正常」或「迟到」
- [ ] **同一天重复打上班卡 → 应被拒绝**（提示当日已打卡）
- [ ] 下班打卡 → 状态更新，首页「今日打卡」显示「已完成」
- [ ] 我的薪资能看到本人薪资记录
- [ ] 直接手输 <http://localhost:8080/employees> → **403 页面**

---

## 三、账号管理模块专项测试（课程项目的「注册」入口）

> 用 `admin` 登录 → 侧边栏「系统管理 → 账号管理」

### 3.1 开通账号（正常流程）

- [ ] 点「开通账号」，员工搜索框留空 → 列出前 20 位在职员工
- [ ] 搜「研发」→ 列出研发相关部门员工；**已有账号的员工应显示「— 已有账号」并置灰不可选**
- [ ] 选一名员工 → 登录名**自动填成该员工工号**
- [ ] 选「不绑定员工（管理员账号）」→ 登录名清空待填，此时不填登录名点开通 → 提示**不绑定员工时必须填写登录名**
- [ ] 角色 + 初始密码留空 → 点「开通」→ 提示成功，列表刷新，新账号出现在顶部区域
- [ ] **用刚开通的账号 + 默认密码 `123456` 登录 → 应成功**

### 3.2 改角色

- [ ] 对刚开通的账号点「改角色」→ 改成「部门经理」→ 保存成功
- [ ] 让该账号重新登录 → 顶部角色徽章变为**部门经理**，侧边栏菜单随之变化
- [ ] 对**自己（admin）**那一行 → 操作列显示「—」（不能改自己角色）

### 3.3 重置密码

- [ ] 点「重置密码」，新密码留空 → 提示**密码已重置为「123456」**
- [ ] 填 `abc`（3 位）→ 提示**密码长度须为 6~20 位**
- [ ] 填 `newpass123` → 提示成功
- [ ] 用旧密码 `123456` 登录 → **失败**；用 `newpass123` → **成功**

### 3.4 启用 / 禁用

- [ ] 点「禁用」→ 二次确认弹窗 → 确定 → 状态列变「禁用」
- [ ] 用被禁用账号登录 → 提示**账号已被禁用（可能已离职），请联系 HR**
- [ ] 点「启用」→ 该账号恢复可登录
- [ ] 对自己的账号 → 操作列「—」，且后端也会拦截（不能禁用自己）

### 3.5 权限边界（用 `hr01` 登录复测）

- [ ] HR 在「开通账号」里选不到「系统管理员 / 人事专员」角色
- [ ] HR 对 `admin` 行没有任何操作按钮
- [ ] 即使绕过前端直接调接口，HR 改管理员账号也会返回 **403**（后端二次校验）

---

## 四、越权与异常测试

- [ ] 未登录状态直接访问 <http://localhost:8080/employees> → 跳转登录页
- [ ] 登录后长时间不操作（会话 8 小时超时）→ 接口返回 401，前端提示「登录已过期，正在跳转…」
- [ ] 员工「离职」操作 → 该员工账号被**触发器自动禁用**，无法登录
- [ ] 「复职」→ 账号被**触发器自动重新启用**，可以登录
- [ ] 连续输错密码 → 只是提示密码错误（课程项目未做登录限流）

---

## 五、测试后如何还原数据

测试过程中新增的账号、员工、薪资记录如需清理，执行：

```sql
-- 1) 查看本次测试新增的账号（按创建时间倒序）
SELECT user_id, username, role_code, enabled, created_at
FROM sys_user ORDER BY user_id DESC LIMIT 10;

-- 2) 删除测试账号（把 'xxx' 换成实际登录名）
DELETE FROM sys_user WHERE username = 'xxx';

-- 3) 若测试时新增了员工，需按外键顺序清理（把 E9999 换成实际工号）
SET @eid = (SELECT emp_id FROM employee WHERE emp_no = 'E9999');
DELETE FROM attendance WHERE emp_id = @eid;
DELETE FROM salary     WHERE emp_id = @eid;
DELETE FROM sys_user   WHERE emp_id = @eid;
DELETE FROM employee   WHERE emp_id = @eid;

-- 4) 若测试生成了某月薪资，按月份清理
DELETE FROM salary WHERE salary_month = '2000-01';

-- 5) 核对基线：账号 1003 条、员工 1002 条、薪资 1962 条
SELECT (SELECT COUNT(*) FROM sys_user)  AS 账号,
       (SELECT COUNT(*) FROM employee)  AS 员工,
       (SELECT COUNT(*) FROM salary)    AS 薪资;
```

> 基线条数：账号 **1003**、员工 **1002**（在职 981）、部门 **25**、职位 **52**、考勤 **24527**、薪资 **1962**（2026-08 与 2026-09 各 981 条）。

---

## 六、本轮已修复的问题（测试时请重点复核）

| # | 问题 | 现象 | 现在应该看到 |
|---|---|---|---|
| 1 | 前端 `api()` 成功但无返回数据时被当成失败 | 离职/复职、部门增删改、职位增删改、改密码等操作**服务端已生效，但页面不弹提示也不刷新** | 上述操作都会正常弹 toast 并刷新列表 |
| 2 | 同上的副作用 | 薪资「生成当月薪资」返回 0 条时提示错乱 | 正确提示「当月薪资已全部生成」 |
| 3 | 存储过程 `deduction` 写入 NULL | 给**没有考勤记录**的月份生成薪资直接报错，整批失败 | 可正常生成，扣款为 0 |
| 4 | AI 模型名用了已下线的旧名 | `deepseek-v4-flash` 已下线 | 改用官方名 `deepseek-flash`，可通过环境变量覆盖 |
| 5 | AI 返回 `content: null` 时被当成有效 SQL | 报出误导性的「SQL 非法」错误 | 会正确识别为空并自动重试 |
| 6 | AI 用旧版 `thinking_mode` 参数控制思考模式 | 该参数只能部分压制推理（实测 158 推理 token），拖慢响应 | 改用现行 API 的 `thinking` + `reasoning_effort: low`，实测 1~1.5 秒返回 |
| 7 | 登录页加载了用不到的 Bootstrap JS | 多一次无用请求 | 已移除（登录页只用 CSS，不依赖 Bootstrap JS） |

---

## 七、环境依赖提醒

| 功能 | 依赖 | 当前状态 |
|---|---|---|
| 全部业务功能 | MySQL 8.0 + `hrms_db` | ✅ 已就绪 |
| **AI 智能查询** | 环境变量 `DEEPSEEK_API_KEY` | ✅ 已配置（未配置时页面会明确提示） |
| AI 模型 / 地址覆盖 | `DEEPSEEK_MODEL` / `DEEPSEEK_BASE_URL` | 可选，默认 `deepseek-flash` |
| 数据库账号密码覆盖 | `DB_USERNAME` / `DB_PASSWORD` | 用户名默认 `root`；**密码无默认值，必须先设置 `DB_PASSWORD` 否则启动失败** |
