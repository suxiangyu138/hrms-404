# 人事管理系统数据库课程设计 —— 后端与数据库交互设计文档

> **项目名称**：人事管理系统数据库课程设计
> **开发小组**：404 Not Found（项目代号 HRMS-404）
> **本文定位**：后端与数据库交互设计文档，含 DeepSeek V4 Flash API 接入设计（见 5.3）与小组特征元素设计（见 4.4）


## 一、总体设计思路

### 1.1 系统分层思想

采用经典的 **表现层-业务层-数据访问层** 三层结构，各层职责明确：

| 层级 | 职责 | 关键技术选型 |
|------|------|-------------|
| **表现层（View）** | 用户交互界面，数据展示与表单提交 | Thymeleaf + Bootstrap，服务端渲染 |
| **控制层（Controller）** | 接收请求，参数校验，调用业务层，返回响应 | Spring MVC |
| **业务层（Service）** | 核心业务逻辑处理，事务管理 | Spring Service + 声明式事务 |
| **数据访问层（DAO）** | 数据库CRUD操作，自定义SQL | MyBatis-Plus |
| **数据库层（DB）** | 数据持久化存储，视图/存储过程/触发器 | MySQL 8.0 |
| **AI 服务层（扩展）** | 自然语言转 SQL（Text to SQL 智能查询） | DeepSeek V4 Flash API（OpenAI 兼容协议） |

> **设计原则**：层与层之间通过接口交互，降低耦合；Controller 不直接操作数据库，所有数据操作必须经过 Service 层。

### 1.2 请求响应流程设计

```
前端页面（Thymeleaf） 
    ↓ (表单提交/AJAX请求)
Controller 接收请求，提取参数
    ↓ (调用Service方法)
Service 执行业务逻辑（事务边界在此）
    ↓ (调用Mapper)
MyBatis-Plus 执行SQL（或调用存储过程）
    ↓ (返回结果)
逐层返回，最终渲染为HTML 或 返回JSON数据
```

### 1.3 前后端交互方式

- **页面导航类请求**：使用表单提交或超链接，由 Controller 返回视图名称，Thymeleaf 渲染完整页面。
- **数据操作类请求**（增删改查）：使用 AJAX 异步请求，后端返回 JSON 格式数据，前端动态刷新局部内容。
- **Text to SQL 请求**：单独设计一个 AJAX 接口，前端输入自然语言，后端返回查询结果表格。

### 1.4 技术选型总览（开发语言为 Java）

系统采用 **Java 语言** 开发，后端使用 Spring 技术栈，各层具体选型如下（分层职责见 1.1）：

| 类别 | 技术选型 | 说明 |
|------|----------|------|
| **开发语言** | **Java** | 全项目统一使用 Java 编写，跨平台、生态成熟，与 Spring 框架天然集成 |
| **构建工具** | Maven | 统一管理依赖、完成项目打包 |
| **后端框架** | Spring Boot + Spring MVC | IoC/AOP 容器、RESTful 接口、声明式事务 |
| **ORM 持久层** | MyBatis-Plus | 简化 CRUD 编写，支持自定义 SQL 与存储过程调用 |
| **前端技术** | Thymeleaf + Bootstrap | 服务端渲染页面，页面不涉及其他后端语言 |
| **数据库** | MySQL 8.0 | 数据持久化，配合视图、存储过程、触发器 |
| **AI 大模型** | DeepSeek V4 Flash API | Text to SQL 自然语言查询，OpenAI 兼容协议（详见 5.3） |
| **调试工具** | Postman + 浏览器开发者工具 | REST API 联调与前端调试 |

> **说明**：本系统所有代码（Controller、Service、Mapper、前端模板逻辑）均为 Java 语言体系，无 Python/Node 等其他语言混用，便于统一管理与答辩演示。

---

## 二、数据库设计思路

### 2.1 概念模型（E-R 图构建思路）

核心实体及关系如下：

| 实体 | 主要属性 | 关系说明 |
|------|----------|----------|
| **部门** | 部门编号、名称、上级部门 | 自关联（上下级树形结构） |
| **职位** | 职位编号、名称、所属部门、编制数 | 部门与职位是 1:N |
| **员工** | 工号、姓名、性别、生日、电话、邮箱、入职日期、状态 | 员工属于一个部门、一个职位 |
| **考勤** | 考勤编号、员工、日期、打卡时间、状态 | 员工与考勤是 1:N |
| **薪资** | 薪资编号、员工、发放月份、基本工资、绩效、实发 | 员工与薪资是 1:N |
| **用户** | 用户编号、员工关联、登录名、密码、角色 | 用户与员工是 1:1，用户属于一个角色 |

> **E-R 图构建要点**：确定实体间联系类型（1:1、1:N、M:N），标注主键和外键，特别关注 **部门自关联**（上下级）和 **用户-员工-角色** 的权限模型。

### 2.2 规范化等级分析与确定

| 范式级别 | 判断依据 | 是否满足 |
|----------|----------|----------|
| **1NF（原子性）** | 所有字段不可再分，如电话、邮箱各存一列 | 满足 |
| **2NF（完全依赖）** | 所有非主属性完全依赖于主键，无部分依赖 | 满足（主键均为单字段） |
| **3NF（无传递依赖）** | 非主属性之间无依赖，如员工不直接存部门名称，只存部门ID | 满足 |
| **BCNF（主键唯一决定）** | 所有决定因素均为候选键 | 满足（无复合主键） |

**结论**：本系统数据库设计达到 **BCNF（Boyce-Codd 范式）**，所有表均以单字段主键为核心，外键关联消除冗余，无传递依赖，符合规范化要求。

> **设计依据**：课程设计要求"基于关系模式规范化理论确定等级"，此处明确给出等级并说明理由。

### 2.3 视图设计思路

**用途**：简化复杂关联查询，提高数据读取效率，同时可作为数据安全控制手段（屏蔽敏感字段）。

| 视图名称 | 涉及表 | 设计目的 |
|----------|--------|----------|
| `v_employee_full_info` | employee + department + position | 展示员工完整信息（含部门名、职位名），避免前端多表联查 |
| `v_attendance_summary` | attendance + employee | 按员工汇总出勤情况，便于统计 |
| `v_salary_history` | salary + employee | 薪资发放历史记录，含员工姓名 |

### 2.4 存储过程设计思路

**用途**：封装复杂业务逻辑，减少网络传输，提高执行效率。

| 存储过程 | 功能说明 | 使用场景 |
|----------|----------|----------|
| `sp_count_employee_by_dept` | 统计指定部门及其子部门总人数 | 部门管理页面展示编制使用率 |
| `sp_generate_monthly_salary` | 根据考勤和职位生成当月薪资 | 每月薪资批量发放 |
| `sp_get_org_tree` | 递归查询部门组织树 | 部门管理树形展示 |

### 2.5 触发器设计思路

**用途**：自动维护数据一致性，实现业务规则自动校验。

| 触发器 | 触发时机 | 功能说明 |
|--------|----------|----------|
| `trg_after_emp_leave` | UPDATE employee（status变为0） | 员工离职时自动禁用关联的用户账号 |
| `trg_after_emp_insert` | INSERT employee | 新增员工时自动创建对应的用户账号（默认密码） |
| `trg_before_salary_insert` | INSERT salary | 插入薪资记录前自动计算实发工资（基本+绩效-扣款） |
| `trg_check_budget` | INSERT/UPDATE position | 检查职位编制数是否超出部门预算上限 |

> **设计要点**：触发器用于保证数据完整性，避免业务层遗漏处理，但不宜过度使用（会影响性能）。

---

## 三、后端交互设计思路

### 3.1 RESTful API 设计规范

**设计原则**：
- 使用名词复数表示资源，如 `/api/employees`、`/api/departments`
- HTTP 方法对应操作语义：GET（查询）、POST（新增）、PUT（更新）、DELETE（删除）
- 使用路径参数标识具体资源：`/api/employees/{id}`
- 统一返回格式，包含状态码、消息、数据三个字段

**返回数据格式约定**：
| 字段 | 类型 | 说明 |
|------|------|------|
| code | Integer | 200成功，其他为错误码 |
| message | String | 提示信息 |
| data | Object/Array | 业务数据或分页对象 |

**分页请求统一处理**：
- 请求参数：page（页码，默认1）、size（每页条数，默认10）
- 返回数据：total（总记录数）、records（当前页数据列表）

### 3.2 主要接口设计思路

| 功能模块 | 设计要点 |
|----------|----------|
| **员工管理** | 支持按姓名、部门、状态等多条件组合查询；删除为逻辑删除（更新status字段），保留数据 |
| **部门管理** | 树形结构展示，支持查询某部门下所有子部门（递归查询）；删除部门前需检查是否有员工 |
| **考勤管理** | 每日只能打卡一次，后端校验是否重复打卡；支持按日期范围查询考勤记录 |
| **薪资管理** | 每月一个员工只能有一条薪资记录；调用存储过程批量生成薪资 |
| **用户权限** | 登录接口返回用户角色信息；操作接口根据角色校验权限（管理员/HR/经理/员工） |
| **Text to SQL** | 调用 DeepSeek V4 Flash API 将自然语言转换为 SQL（接入设计见 5.3）；仅允许SELECT查询；对生成的SQL做安全性过滤；结果转为表格格式返回 |

### 3.3 异常处理设计

**统一异常拦截思路**：
- 定义全局异常处理器，捕获业务异常、参数校验异常、数据库异常
- 返回统一的错误格式，避免前端处理各种异常类型
- 日志记录异常堆栈，便于调试

**常见异常类型**：
| 异常场景 | 处理方式 |
|----------|----------|
| 请求参数格式错误 | 返回400，提示具体字段错误 |
| 资源不存在（如员工ID无效） | 返回404，提示资源未找到 |
| 业务规则冲突（如重复打卡） | 返回409，提示冲突原因 |
| 数据库操作异常（如外键冲突） | 返回500，友好提示后记录日志 |
| 权限不足 | 返回403，提示无操作权限 |

---

## 四、前端界面设计思路

### 4.1 页面布局设计

采用 **左右结构** 或 **上下结构** 的经典后台管理布局：

| 区域 | 内容 |
|------|------|
| **顶部导航栏** | 系统Logo、当前用户信息、退出登录 |
| **左侧菜单栏** | 按角色动态显示菜单（部门管理、员工管理、考勤管理、薪资管理、Text to SQL查询） |
| **右侧主内容区** | 展示当前功能页面，包含查询条件、操作按钮、数据表格 |

### 4.2 核心功能页面设计思路

| 页面 | 设计要点 |
|------|----------|
| **员工列表页** | 顶部查询条件（姓名模糊查询、部门下拉筛选、状态筛选）；中间操作按钮（新增、导出）；底部分页表格展示员工信息 |
| **员工编辑弹窗** | 使用模态框（Modal）展示表单，包含所有员工字段；部门使用下拉选择，职位根据部门动态加载 |
| **部门管理页** | 使用树形组件展示部门层级；右键或按钮实现增删改操作 |
| **考勤打卡页** | 展示当月日历或打卡记录列表；一个醒目打卡按钮，打卡后显示今日打卡状态 |
| **薪资发放页** | 按月份筛选；显示员工薪资列表；"生成薪资"按钮调用存储过程 |
| **Text to SQL页** | 输入框输入自然语言问题；提交后展示生成的SQL语句和查询结果表格；提供示例问题供参考 |

### 4.3 交互反馈设计

- **操作成功/失败**：使用 Toast 轻提示，不中断用户操作
- **加载状态**：提交表单或请求数据时显示加载遮罩或进度条
- **表单验证**：前端进行基础验证（必填、格式），后端进行二次验证
- **确认操作**：删除等危险操作弹出确认框

### 4.4 小组特征元素设计（404 Not Found 主题）

> **设计理念**：组名取自 HTTP 状态码 404（Not Found）。开发时把"404"作为小组名片融入系统各处——它恰好与"错误页、空数据、资源不存在"等场景语义吻合，答辩时自然形成记忆点。原则：**点到为止，不干扰正常业务操作**。

| 特征元素 | 出现位置 | 具体设计 / 文案 |
|----------|----------|----------------|
| 项目代号 | 文档、代码注释 | HRMS-404（Human Resource Management System + 404 Not Found） |
| 品牌 Logo | 顶栏左侧、登录页 | "404" 数字标识 + 简单人形图形，悬停提示"404 Not Found 小组" |
| 页签标题 | 浏览器 `<title>` | 统一后缀格式：`xx管理 - HRMS-404` |
| 小组徽标 | 顶栏右侧 | 小徽章"404"，悬停显示组名全称 |
| 登录页署名 | 登录框下方 | "404 Not Found 小组 · 人事管理系统数据库课程设计" |
| 自定义 404 页 | 访问不存在的路由 | 大号 "404 Not Found" + 文案"你要找的页面不存在，已被按'离职'流程处理" + "返回首页"按钮 |
| 空数据占位 | 各列表页查询无记录 | "404：暂未找到符合条件的记录，去新增一条吧" |
| Toast 点缀 | 全局操作提示 | 删除成功提示追加后缀"（已从列表 404）" |
| 页脚 | 所有页面底部 | "© 2026 404 Not Found 小组 · 人事管理系统数据库课程设计" |
| 控制台彩蛋 | 浏览器开发者工具 | `console.log('404 Not Found：页面已找到，系统运行正常 :)')` |
| 关于弹窗 | 顶栏"关于"入口 | 展示组名、口号与成员名单（成员名单定稿后补充） |

> **后端配套**：全局异常处理中对"资源不存在"统一返回 HTTP 404 状态码与提示"404：资源不存在"（见 3.3），与前端 404 页面风格一致，形成前后端统一的"404"品牌闭环。

---

## 五、Text to SQL 功能设计思路

### 5.1 功能定位

这是一个 **扩展功能**，核心目标：
1. 展示数据库课程设计中对 SQL 语法的理解
2. 体验 AI 大模型与数据库结合的实践场景
3. 作为系统亮点，增强项目展示效果

### 5.2 技术实现思路

**整体流程**：
```
用户输入自然语言 → 构建Prompt（包含表结构信息） → 调用 DeepSeek V4 Flash API（/chat/completions） → 解析返回的SQL → 安全性校验 → 执行SQL → 返回结果
```

**关键设计点**：

| 设计点 | 思路说明 |
|--------|----------|
| **Prompt 设计** | 在系统提示词中完整描述所有表结构及字段含义，让AI理解业务上下文；要求只返回SQL语句，不加解释 |
| **表结构传递** | 将数据库所有表的DDL或字段描述作为上下文嵌入Prompt，确保AI生成正确的表名和字段名 |
| **安全性控制** | 生成的SQL必须强制以 SELECT 开头；使用正则过滤危险关键词（DROP、DELETE、UPDATE、INSERT、ALTER等）；限制返回行数（如 LIMIT 100） |
| **结果展示** | 将查询结果动态渲染为 HTML 表格；列名使用字段注释（中文名） |
| **错误处理** | AI生成SQL失败或执行失败时，返回友好的错误提示，并建议用户重新表述问题 |

### 5.3 技术选型：DeepSeek V4 Flash API 接入设计

**选型理由**：

| 考虑维度 | 说明 |
|----------|------|
| 生态兼容 | DeepSeek API 完全兼容 OpenAI Chat Completions 协议，Java 后端可直接用 openai-java SDK 或 HttpClient 调用，无需专用 SDK |
| 性价比 | flash 轻量档定价远低于旗舰模型，适合课程项目反复调试；按量计费，开通门槛低 |
| 上下文窗口 | 原生支持 1M token 上下文，全部表结构 DDL + 字段注释可一次性放入系统提示词 |
| 生成可控 | 支持 temperature、seed、JSON 模式输出，利于保证 SQL 生成的确定性与易解析性 |
| 可迁移性 | 模型开源（MoE 架构），若预算受限可平滑切换到本地推理或其它 OpenAI 兼容网关，业务代码零改动 |

**API 接入基础信息**：

| 配置项 | 值 |
|--------|-----|
| 接口 Base URL | `https://api.deepseek.com` |
| 调用端点 | `POST /chat/completions`（OpenAI 兼容格式） |
| 模型名称 | `deepseek-v4-flash` |
| 认证方式 | 请求头 `Authorization: Bearer <API_KEY>` |
| API Key | 在 platform.deepseek.com 申请，通过环境变量注入，禁止硬编码进代码或前端页面 |

**后端接入设计（Spring Boot）**：

| 设计点 | 思路说明 |
|--------|----------|
| 密钥管理 | `application.yml` 中只写 `${DEEPSEEK_API_KEY}`，实际值存放于部署环境变量 |
| 请求封装 | 新增 `TextToSqlService`，通过 HttpClient（或 openai-java SDK）封装 `generateSql(question)` 方法；连接超时 5s、读取超时 30s，失败自动重试 1 次后降级 |
| 关键参数 | `temperature=0`（保证 SQL 确定性输出）、`max_tokens=4000`、`thinking_mode=non-thinking`（强制关闭思考模式，避免推理 token 耗尽预算导致 content 为空——曾引发"AI 未返回有效 SQL"问题）；空返回时自动重试最多 3 次并记录原始响应便于排查 |
| 系统提示词 | 内置全部表 DDL 及字段中文注释；硬性约束：只输出 JSON、只能 SELECT、禁止访问 information_schema / mysql 系统库、自动附加行数限制 |
| 响应解析 | 解析 `choices[0].message.content` 为 JSON 并提取 `sql` 字段；解析失败时提示用户重新表述问题 |
| 错误降级 | 接口返回 401（密钥失效）、402（余额不足）、429（限流）或网络超时时，页面提示"AI 服务暂不可用"，不影响系统其余功能 |

**调用链路示意**：

```
页面提问 → TextToSqlService.generateSql(question)
    → 组装 messages（system: 表结构DDL+输出约束 / user: 自然语言问题）
    → POST https://api.deepseek.com/chat/completions
    → 校验响应并提取 sql → SQL 白名单校验（见 5.5） → 执行查询 → 返回表格
```

### 5.4 用户交互设计

- 提供几个 **示例问题** 按钮，点击自动填入输入框（如："查询所有销售部的员工"、"统计各部门人数"）
- 提交后展示 **生成过程**：先显示AI生成的SQL语句，再显示查询结果
- 如果SQL执行结果为空，提示"没有找到匹配的数据"
- 支持结果表格的 **导出 Excel（.xlsx）** 功能（Apache POI 生成）

### 5.5 安全性约束

| 约束项 | 措施 |
|--------|------|
| 只允许查询 | 拦截所有非 SELECT 语句 |
| 禁止访问系统表 | 过滤 information_schema、mysql 等库 |
| 行数限制 | 强制添加 LIMIT 100，防止大数据量查询 |
| 超时控制 | 设置查询超时时间，防止慢查询阻塞 |
| 并发限制 | 同一用户限制请求频率，防止滥用 |

---

## 六、数据库安全实现思路

### 6.1 权限控制设计

**用户角色定义**：

| 角色 | 权限范围 |
|------|----------|
| 系统管理员 | 所有功能 + 用户管理 + 系统配置 |
| HR专员 | 员工管理、职位管理、部门管理、考勤查看、薪资管理 |
| 部门经理 | 查看本部门员工信息、审批考勤 |
| 普通员工 | 查看个人信息、打卡签到 |

**实现思路**：
- 基于角色的访问控制（RBAC）
- 每次请求拦截器中校验用户角色和所访问资源的匹配关系
- 数据库层面视图可以限制敏感字段（如薪资信息仅HR可查看）

### 6.2 数据安全措施

| 措施 | 说明 |
|------|------|
| 密码加密 | 用户密码使用 BCrypt 或 MD5加盐存储 |
| SQL注入防护 | MyBatis-Plus 使用 #{} 参数绑定；Text to SQL 做独立安全过滤 |
| 敏感数据脱敏 | 员工手机号、邮箱在前端展示时可部分隐藏 |
| 操作日志 | 记录关键操作（删除员工、修改薪资）的操作用户和时间 |

---

## 七、开发流程与阶段规划

| 阶段 | 时间 | 主要任务 | 交付物 |
|------|------|----------|--------|
| **需求分析与设计** | 第1周 | 确定模块功能、绘制E-R图、设计表结构、确定范式等级 | E-R图、数据字典、表结构设计文档 |
| **数据库搭建** | 第2周 | 创建数据库、表、视图、存储过程、触发器 | 完整建表DDL脚本 |
| **后端基础搭建** | 第3周 | Spring Boot项目初始化、配置MyBatis-Plus、编写基础CRUD | 项目骨架、基础Mapper |
| **核心业务实现** | 第4周 | 完成所有模块Service + Controller接口开发 | 完整的RESTful API |
| **前端界面开发** | 第5周 | Thymeleaf模板开发，实现所有功能页面 | 完整前端页面 |
| **Text to SQL扩展** | 第6周 | AI接口接入、安全性校验、前端交互页面 | AI查询功能模块 |
| **测试与完善** | 第7周 | 功能测试、Bug修复、性能优化 | 测试报告 |
| **文档与答辩准备** | 第8周 | 整理实验报告、个人总结、制作PPT | 完整项目资料 |

---

## 八、文档编写思路（供你写入实验报告）

### 8.1 实验报告应包含的章节

1. **需求分析**：系统目标、功能需求、用户角色
2. **概念结构设计**：E-R图构建过程（实体识别 → 属性确定 → 关系确定）
3. **逻辑结构设计**：E-R图转关系模式、规范化分析（确定BCNF等级）
4. **物理结构设计**：建表语句、索引设计、存储过程、触发器
5. **数据库实现**：视图定义、安全性配置
6. **系统实现**：后端架构、前端界面、核心功能截图
7. **Text to SQL功能**：设计思路（DeepSeek V4 Flash API 接入）、交互流程、安全性保障
8. **总结与收获**：遇到的问题、解决方案、个人贡献

### 8.2 个人总结报告应包含的内容

- 你在项目组中的具体职责（后端、前端、数据库）
- 完成了哪些核心功能模块
- 技术选型的学习与运用（MyBatis-Plus、Thymeleaf、DeepSeek V4 Flash API 等）
- 遇到的困难及解决过程
- 对数据库理论（范式、视图、存储过程、触发器）的实践认识
- 团队协作中的角色与贡献

---
